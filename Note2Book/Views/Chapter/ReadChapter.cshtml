@using Newtonsoft.Json
@model Note2Book.ViewModels.ChapterViewModel

<div class="chapter-reading">
    <div class="title-container">
        <h2>@Model.Chapter.Title</h2>
        <a href="@Url.Action("Book", "Book", new { id = Model.Chapter.Book.Id })" class="btn btn-info return-btn">Вернуться к книге</a>
    </div>

    @if (!string.IsNullOrEmpty(Model.PageText))
    {
    <div id="chapterContent">
        <!-- Вставляем текст с цитатами -->
        @{
        var textWithQuotes = Model.PageText;
        foreach (var quote in Model.Citations)
        {
        // Заменяем цитаты на кликабельные элементы
        textWithQuotes = textWithQuotes.Replace(quote.Text,
        $"<span class='highlighted-quote' data-quote-id='{quote.Id}' title='Нажмите, чтобы увидеть комментарий'>{quote.Text}</span>");
        }
        }

        <!-- Выводим измененный текст главы -->
        <div class="chapter-text" style="margin-bottom: 10px;">
            @Html.Raw(textWithQuotes)
        </div>
    </div>
    }

    <div class="navigation-buttons">
        @if (Model.PreviousChapter != null)
        {
        <a href="@Url.Action("ReadChapter", "Chapter", new { chapterId = Model.PreviousChapter, pageNumber = 1 })"
           class="btn btn-secondary">Предыдущая глава</a>
        }

        @if (Model.CurrentPage > 1)
        {
        <a href="@Url.Action("ReadChapter", "Chapter", new { chapterId = Model.Chapter.Id, pageNumber = Model.CurrentPage - 1 })"
           class="btn btn-primary">Предыдущая страница</a>
        }

        @if (Model.CurrentPage < Model.TotalPages)
        {
        <a href="@Url.Action("ReadChapter", "Chapter", new { chapterId = Model.Chapter.Id, pageNumber = Model.CurrentPage + 1 })"
           class="btn btn-primary">Следующая страница</a>
        }

        @if (Model.NextChapter != null)
        {
        <a href="@Url.Action("ReadChapter", "Chapter", new { chapterId = Model.NextChapter, pageNumber = 1 })"
           class="btn btn-success">Следующая глава</a>
        }
    </div>
</div>

<!-- Модальное окно для отображения комментария к цитате -->
<div class="modal" id="commentModal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Комментарий к цитате</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p id="quoteComment"></p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Закрыть</button>
            </div>
        </div>
    </div>
</div>

<!-- Модальное окно для добавления новой цитаты -->
<div class="modal" id="addQuoteModal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Добавить новую цитату</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div>
                    <label for="quoteText">Цитата:</label>
                    <textarea id="quoteText" class="form-control"></textarea>
                </div>
                <div>
                    <label for="quoteCommentInput">Комментарий:</label>
                    <textarea id="quoteCommentInput" class="form-control"></textarea>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" id="saveQuoteBtn">Сохранить</button>
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Закрыть</button>
            </div>
        </div>
    </div>
</div>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

<script>
    $(document).ready(function () {
        // Обработчик клика по цитате
        $('.highlighted-quote').click(function () {
            var quoteId = $(this).data('quote-id');
            var comment = '';

            // Поиск комментария по ID цитаты
            var citations = @(Html.Raw(JsonConvert.SerializeObject(Model.Citations)));
            var selectedCitation = citations.find(c => c.Id == quoteId);

            // Если комментарий найден
            if (selectedCitation) {
                comment = selectedCitation.Comment;
            } else {
                comment = 'Комментарий не найден';
            }

            // Отображаем комментарий в модальном окне
            $('#quoteComment').text(comment);
            $('#commentModal').modal('show');
        });

        // Обработчик выделения текста
        $('#chapterContent').mouseup(function () {
            var selectedText = window.getSelection().toString();
            if (selectedText.length > 0) {
                $('#addQuoteModal').modal('show');
            }
        });

        // Обработчик сохранения новой цитаты
        // Обработчик сохранения новой цитаты
        $('#saveQuoteBtn').click(function () {
            var quoteText = $('#quoteText').val();
            var quoteComment = $('#quoteCommentInput').val();

            // Здесь нужно будет добавить AJAX запрос для сохранения новой цитаты на сервер
            // Пример AJAX запроса:
            $.ajax({
                type: "POST",
                url: '@Url.Action("CreateQuote", "Quote")', // URL для сохранения новой цитаты
                data: {
                    text: quoteText,
                    comment: quoteComment,
                    chapterId: @Model.Chapter.Id // Укажите идентификатор главы
                },
                success: function (response) {
                    // Обновите список цитат или выполните другие действия
                    alert('Цитата добавлена успешно!');
                    $('#addQuoteModal').modal('hide');
                    $('#quoteText').val('');
                    $('#quoteCommentInput').val('');
                    // Вы можете обновить страницу или добавить новую цитату в текст
                },
                error: function () {
                    alert('Произошла ошибка при добавлении цитаты.');
                }
            });
        });
    });
</script>


<style>
    .chapter-reading {
        max-width: 800px;
        margin: 0 auto;
        padding: 20px;
        background-color: #f9f9f9;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .title-container {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 20px;
    }

    .chapter-text {
        text-align: justify;
        line-height: 1.6;
        font-size: 1.1em;
        color: #333;
    }

    .highlighted-quote {
        background-color: rgba(106, 90, 205, 0.3);
        padding: 3px;
        border-radius: 5px;
        cursor: pointer;
    }

    .highlighted-quote:hover {
        background-color: rgba(106, 90, 205, 0.5);
    }

    .return-btn {
        padding: 8px 16px;
        background-color: #6A5ACD;
        border-radius: 5px;
        color: white;
        text-decoration: none;
    }

    .return-btn:hover {
        background-color: #483D8B;
        color: white;
    }

    .navigation-buttons {
        display: flex;
        flex-wrap: wrap;
        justify-content: space-between;
        margin-top: 20px;
        gap: 10px;
    }

    .navigation-buttons a {
        display: inline-block;
        padding: 8px 16px;
        text-decoration: none;
        border-radius: 5px;
        color: white;
    }

    .btn-primary {
        background-color: #4B0082;
    }

    .btn-primary:hover {
        background-color: #3A006F;
    }

    .btn-success {
        background-color: #6A5ACD;
    }

    .btn-success:hover {
        background-color: #483D8B;
    }

    .btn-secondary {
        background-color: #808080;
    }

    .btn-secondary:hover {
        background-color: #696969;
    }

    .btn-info {
        background-color: #7B68EE;
    }

    .btn-info:hover {
        background-color: #6A5ACD;
    }
</style>
